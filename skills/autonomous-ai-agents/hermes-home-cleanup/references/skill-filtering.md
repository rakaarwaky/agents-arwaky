# Role-based skill filtering (local mirror)

Canonical: `skills/devops/hermes-profiles/references/skill-filtering.md`.
Prune a profile by adding skill names to its `skills.disabled` list
in `~/.hermes/profiles/<p>/config.yaml` — never delete or move skill
dirs. Load the canonical file for install/apply/embed distinctions
and the enforcement details.
