"""AES feature modules — orchestration agents for the agents-arwaky ecosystem.

Each subpackage is a feature module following the AES 7-layer layout::

    modules/<feature>/
        src/   capabilities_*, agent_*_orchestrator, root_*_container, surface_*
        tests/
    modules/shared/src/<domain>/   taxonomy_* + contract_* (shared cross-feature)
"""
